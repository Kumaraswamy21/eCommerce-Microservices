server:
  port: 8888

spring:
  application:
    name: configserver
  profiles:
    active: native
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    virtual-host: ${RABBITMQ_VHOST:guest}
    connection-timeout: 30000
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users\kumar\Springboot\Microservices\ecom-microservices\configserver\src\main\resources\config

management:
  endpoints:
    web:
      exposure:
        include: "*"

